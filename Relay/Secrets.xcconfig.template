// Relay/Secrets.xcconfig.template
// Copy this to Secrets.xcconfig and fill in real values.
// Debug builds seed the Intervals.icu connection from these (Settings → Developer on intervals.icu).

INTERVALS_ATHLETE_ID = YOUR_ATHLETE_ID
INTERVALS_API_KEY = YOUR_API_KEY
